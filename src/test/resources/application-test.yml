spring:
  datasource:
    url: jdbc:tc:postgresql:17:///budget_control
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration